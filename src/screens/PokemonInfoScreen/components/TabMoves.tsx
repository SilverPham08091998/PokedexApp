import React from "react";
import { View } from "react-native";
import { CText } from "@/components";

interface Props {}

const TabMoves: React.FC<Props> = (props: Props) => {
  return (
    <View>
      <CText>ABC</CText>
    </View>
  );
};

export default TabMoves;
